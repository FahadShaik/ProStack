getName();

function getName() {
  console.log("Hello");
}

/** here the callstack will keep the entire function into it which means the memory will already be allocated before the running of code itself and by giving the invocation it just calls up the function. */
